# Lessora Admin Portal

A dedicated React + TypeScript admin web portal for the Lessora platform.

## Development

1. Install dependencies with `npm install`.
2. Start the development server with `npm run dev`.
3. The app proxies `/api/*` requests to the shared backend running on `http://localhost:4000`.

## Build

Run `npm run build` to generate production assets.
